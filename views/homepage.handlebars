{{> navigation }}
<h1>Homepage</h1>
{{#each posts}}
    <div>
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <small>By {{this.user.username}} on {{this.createdAt}}</small>
    </div>
    {{/each}}